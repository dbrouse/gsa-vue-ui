// Tailwind v4 preset for consumers
// Usage in consumer app: import preset from '@gsa/vue-ui/tailwind-preset'

/** @type {import('tailwindcss').Config} */
export default {
  content: [],
  // Tailwind v4 uses CSS @theme for theming instead of theme config
  // See styles.css for color and design token definitions
  darkMode: "class",
};
